name: Parent Pipeline
on: [workflow_dispatch]
jobs: 
  build-test-run:
    uses: ./.github/workflows/Parents.yml
    secrets:
      DATABASE_USERNAME: ${{ secrets.DB_USER}}
      DATABASE_PASSWORD: ${{ secrets.DB_PASS}}
